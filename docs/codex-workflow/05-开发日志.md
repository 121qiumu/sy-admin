# 开发日志

## 2026-04-15 - Step 1

### 本步任务

- 从后端项目中提炼前端可直接使用的接口文档
- 建立 `docs/codex-workflow` 工作记录体系

### 实际完成

1. 确认管理后台接口统一前缀为 `/admin`
2. 确认 token 通过 `Authorization` 请求头直接传原始值
3. 确认标准成功码 `1000` 与通用分页结构
4. 梳理登录、用户、角色、部门、菜单、参数、日志、字典、上传、文件空间等核心接口
5. 产出首批接口总览、示例文档和前端对接约定

### 关键提醒

- `verifyCode` 前端按字符串处理更稳妥
- `permmenu` 返回平铺菜单数组，前端需自行组树
- `/admin/base/comm/upload` 成功后直接返回字符串 URL
- `html` 类接口返回原始 HTML 文本，不走标准 JSON data 解析

## 2026-04-15 - Step 2

### 本步任务

- 初始化前端工程骨架
- 完成工程化配置
- 不进入业务页面开发

### 项目落点

- `D:\My_Project\sy-admin\sy-admin-vue`

### 实际完成

1. 创建 `sy-admin-vue` 前端项目目录与基础文件
2. 安装 Vue 3、Vite、Vue Router、Pinia、Axios、Element Plus
3. 配置 Vite、路径别名、环境变量和开发代理
4. 配置路由基础结构、Layout 壳子、占位工作台与 404 页面
5. 配置 Pinia、`auth/app` 基础 store
6. 配置 Axios 客户端、请求响应拦截器和 API 模块目录
7. 配置 Less 全局样式入口、CSS Variables 和明暗主题切换
8. 配置 Element Plus 组件、图标、样式按需引入
9. 配置 ESLint、Prettier、`.editorconfig`
10. 配置 Husky、`lint-staged` 和 `pre-commit` hook
11. 配置 `rollup-plugin-visualizer` 开关与 `pnpm analyze`
12. 编写项目 README 与初始化记录文档

### 本步新增文档

- [06-前端工程初始化记录.md](./06-前端工程初始化记录.md)
- [sy-admin-vue/README.md](../../sy-admin-vue/README.md)

### 验证结果

- `pnpm lint`：通过
- `pnpm build`：通过
- Husky `prepare`：通过，已生成 `.husky/_`

### 下一步建议

- Step 3 先补齐环境变量与正式请求层，再进入真实认证联调

## 2026-04-15 - Step 3

### 本步任务

- 设计并落地运行环境变量
- 重构 axios 请求层
- 集成 `unplugin-auto-import`
- 不进入业务页面开发

### 项目落点

- `D:\My_Project\sy-admin\sy-admin-vue`

### 实际完成

1. 在 `vite.config.mjs` 中集成 `unplugin-auto-import`
2. 支持自动导入 `vue`、`vue-router`、`pinia` 以及 Element Plus 反馈 API
3. 设计并补齐 `.env.development`、`.env.test`、`.env.production`
4. 同步补齐 `.env.example` 与 `.env.analyze`
5. 新增 `build:test` 脚本，支持测试环境构建
6. 重构 `src/config/index.js`，统一管理请求相关环境变量读取
7. 重构 `src/http` 目录，拆分为 `client`、`interceptors`、`request`、`status`、`utils`
8. 实现 token 自动注入、统一错误提示、业务码统一处理、超时处理
9. 实现 `401/403/500` 等状态处理逻辑
10. 预留 `request.text`、`request.upload`、`request.download`、`request.downloadToFile`
11. 按 Step 1 对接约定调整 `src/api/modules/auth.js` 和 `src/api/modules/app.js`
12. 编写 Step 3 说明文档

### 本步新增文档

- [07-请求层与环境变量说明.md](./07-请求层与环境变量说明.md)

### 验证结果

- `pnpm lint`：通过
- `pnpm build`：通过

### 下一步建议

- Step 4 接入验证码、登录、用户信息与认证闭环

## 2026-04-15 - Step 4

### 本步任务

- 基于真实接口完成后台系统最基础的登录闭环
- 完成登录页、登录状态、用户信息、退出登录和白名单处理
- 不进入菜单和动态路由开发

### 项目落点

- `D:\My_Project\sy-admin\sy-admin-vue`

### 实际完成

1. 开发基于 Element Plus 的登录页，并支持明暗主题切换
2. 完成用户名、密码、验证码三项表单校验
3. 对接 `/admin/base/open/captcha` 和 `/admin/base/open/login`
4. 在 Pinia `auth` store 中补齐 token、refreshToken、userInfo、profileLoaded 等认证状态
5. 完成登录成功后拉取 `/admin/base/comm/person`
6. 完成退出登录接口调用与客户端登录态清理
7. 完成登录成功跳转、登录页回跳和路由白名单基础处理
8. 完成应用启动后的用户信息自动补拉
9. 更新工作台与顶部栏，展示当前登录用户和退出入口
10. 编写 Step 4 开发记录文档
11. 将登录页升级为双栏企业风格布局，补充左侧纯 CSS 动画视觉区
12. 将 `src/assets/images/bg.svg` 接入登录页左侧视觉区，并修复登录页/顶部栏主题切换无效问题

### 本步新增文档

- [08-登录认证开发记录.md](./08-登录认证开发记录.md)

### 真实联调结果

- `GET http://127.0.0.1:8001/admin/base/open/captcha`：返回 `code = 1000`，可正常拿到 `captchaId + base64 svg`
- `GET http://127.0.0.1:8001/admin/base/comm/person` 在无 token 下：返回 `HTTP 401`，响应体为 `{"code":1001,"message":"登录失效或无权限访问~"}`
- 后端本地初始化数据中确认存在 `admin` 用户，默认密码哈希对应 `123456`

### 验证结果

- `pnpm lint`：通过
- `pnpm build`：通过

### 过程说明

- `pnpm build` 在沙箱内依旧触发 `spawn EPERM`
- 之后在放开环境中完成生产构建校验
- 本步没有提前接入菜单权限和动态路由
- 登录页视觉升级仅调整 `src/views/login/index.vue` 的界面结构与样式，不影响既有认证链路
- 后续补充已将主题切换改为显式事件处理，并保持认证逻辑不变

### 当前结论

- 前端已经具备最基础的登录认证闭环能力
- 当前认证链路为：验证码 -> 登录 -> 存 token -> 拉当前用户 -> 进入工作台
- 401 登录失效已能统一清理登录态并回到登录页
- refresh token 自动续期队列仍留在后续更完整的权限联调步骤处理

### 下一步建议

- Step 5 接入真实菜单权限、动态路由与后台壳子联调

## 2026-04-16 - Step 5

### 本步任务

- 基于真实接口完成菜单权限、路由权限、按钮权限闭环
- 完成动态路由、权限守卫、菜单渲染、403/404 页面
- 不进入复杂业务模块开发

### 项目落点

- `D:\My_Project\sy-admin\sy-admin-vue`

### 实际完成

1. 对接 `/admin/base/comm/permmenu`，将返回的 `menus + perms` 作为权限系统单一来源
2. 基于 `parentId`、`type`、`router`、`icon`、`orderNum`、`viewPath`、`keepAlive`、`isShow` 等字段完成前端菜单树转换
3. 新增 `permission` store，集中管理权限码、原始菜单、菜单树、侧边栏菜单、可访问路径与动态路由状态
4. 基于真实菜单数据完成动态路由注入，并将动态页面统一挂载到 Layout 壳子之下
5. 完成路由守卫增强，支持登录态校验、权限数据初始化、403/404 分流、受保护路由刷新直达
6. 完成侧边栏菜单按权限动态渲染，并补齐图标映射
7. 封装 `v-permission` 指令与 `hasPermission` 判断函数，支持按钮级权限控制
8. 新增 403 页面、重构 404 页面，并接入未授权访问处理
9. 将登录失败、退出登录、401 失效与权限态清理打通，避免动态路由与菜单残留
10. 新增通用权限占位页 `src/views/permission/workspace.vue`，承接本步权限闭环，不提前进入真实业务页面
11. 修复构建阶段菜单图标依赖问题，改为复用项目已配置的 `unplugin-icons`

### 本步新增文档

- [09-权限系统开发记录.md](./09-权限系统开发记录.md)

### 对接依据

- Step 1 文档中对 `/admin/base/comm/permmenu` 的接口约定
- 后端菜单字段约定文档 `03-前端对接约定.md`
- 后端权限相关源码中对 `menus` 平铺结构、`perms` 权限数组、菜单类型定义的实际实现

### 验证结果

- `pnpm lint`：通过
- `pnpm build`：通过

### 过程说明

- 沙箱内执行 Node 相关命令仍会遇到 `EPERM`
- 已切换到放开权限环境完成 `lint/build` 校验
- 首次构建失败原因为 `src/layout/menu-icons.js` 直接引用了未安装的 `@element-plus/icons-vue`
- 已改为使用项目现有的 `unplugin-icons` 虚拟图标导入，随后重新构建通过

### 当前结论

- 前端已具备真实菜单、页面、按钮三级权限基础设施
- 权限入口链路已形成：登录 -> 获取用户信息 -> 获取 `permmenu` -> 注入动态路由 -> 渲染菜单 -> 控制页面与按钮权限
- 当前未进入复杂业务模块，真实业务页面可在后续步骤逐个替换权限占位页

### 下一步建议

- Step 6 进入系统管理首个真实业务模块，优先建议“部门管理”
