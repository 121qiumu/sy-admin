# 开发日志

## 2026-04-15 - Step 1

### 本步任务

- 从后端项目中提炼前端可直接使用的接口文档
- 建立 `docs/codex-workflow` 工作记录体系

### 实际完成

1. 确认了管理后台接口统一前缀 `/admin`
2. 确认了 token 通过 `Authorization` 请求头直接传原始值
3. 确认了标准成功码 `1000` 与通用分页结构
4. 梳理了登录、用户、角色、部门、菜单、参数、日志、字典、上传、文件空间等核心接口
5. 产出了首批接口总览、示例文档和前端对接约定

### 关键提醒

- `verifyCode` 前端按字符串处理更稳妥
- `permmenu` 返回平铺菜单数组，前端需自行组树
- `/admin/base/comm/upload` 成功后直接返回字符串 URL
- `html` 类接口返回原始 HTML 文本，不走标准 JSON data 解析

## 2026-04-15 - Step 2

### 本步任务

- 初始化前端工程骨架
- 完成工程化配置
- 不进入业务页面开发

### 项目落点

- `D:\My_Project\sy-admin\sy-admin-vue`

### 实际完成

1. 创建了 `sy-admin-vue` 前端项目目录与基础文件
2. 安装了 Vue 3、Vite、Vue Router、Pinia、Axios、Element Plus
3. 配置了 Vite、路径别名、环境变量和开发代理
4. 配置了路由基础结构、Layout 壳子、占位工作台与 404 页面
5. 配置了 Pinia、`auth/app` 基础 store
6. 配置了 Axios 客户端、请求/响应拦截器和 API 模块目录
7. 配置了 Less 全局样式入口、CSS Variables 和明暗主题切换
8. 配置了 Element Plus 组件、图标、样式按需引入
9. 配置了 ESLint、Prettier、`.editorconfig`
10. 配置了 Husky、`lint-staged` 和 pre-commit hook
11. 配置了 `rollup-plugin-visualizer` 开关与 `pnpm analyze`
12. 编写了项目 README 与初始化记录文档

### 本步新增文档

- [06-前端工程初始化记录.md](./06-前端工程初始化记录.md)
- [sy-admin-vue/README.md](../../sy-admin-vue/README.md)

### 验证结果

- `pnpm lint`：通过
- `pnpm build`：通过
- Husky `prepare`：通过，已生成 `.husky/_`

### 下一步建议

- Step 3 先补齐环境变量与正式请求层，再进入真实认证联调

## 2026-04-15 - Step 3

### 本步任务

- 设计并落地运行环境变量
- 重构 axios 请求层
- 集成 `unplugin-auto-import`
- 不进入业务页面开发

### 项目落点

- `D:\My_Project\sy-admin\sy-admin-vue`

### 实际完成

1. 在 `vite.config.mjs` 中集成了 `unplugin-auto-import`
2. 支持自动导入 `vue`、`vue-router`、`pinia` 以及 Element Plus 反馈 API
3. 设计并补齐了 `.env.development`、`.env.test`、`.env.production`
4. 同步补齐了 `.env.example` 与 `.env.analyze`
5. 新增 `build:test` 脚本，支持测试环境构建
6. 重构了 `src/config/index.js`，统一管理请求相关环境变量读取
7. 重构了 `src/http` 目录，拆分为 `client`、`interceptors`、`request`、`status`、`utils`
8. 实现了 token 自动注入、统一错误提示、业务码统一处理、超时处理
9. 实现了 `401/403/500` 等状态处理逻辑
10. 预留了 `request.text`、`request.upload`、`request.download`、`request.downloadToFile`
11. 按 Step 1 对接约定调整了 `src/api/modules/auth.js` 和 `src/api/modules/app.js`
12. 编写了 Step 3 说明文档

### 本步新增文档

- [07-请求层与环境变量说明.md](./07-请求层与环境变量说明.md)

### 验证结果

- `pnpm lint`：通过
- `pnpm build`：通过

### 下一步建议

- Step 4 接入验证码、登录、刷新 token、用户信息与认证闭环

## 2026-04-15 - Step 4

### 本步任务

- 基于真实接口完成后台系统最基础的登录闭环
- 完成登录页、登录状态、用户信息、退出登录和白名单处理
- 不进入菜单和动态路由开发

### 项目落点

- `D:\My_Project\sy-admin\sy-admin-vue`

### 实际完成

1. 开发了基于 Element Plus 的登录页，并支持明暗主题切换
2. 完成了用户名、密码、验证码三项表单校验
3. 对接了 `/admin/base/open/captcha` 和 `/admin/base/open/login`
4. 在 Pinia `auth` store 中补齐了 token、refreshToken、userInfo、profileLoaded 等认证状态
5. 完成了登录成功后拉取 `/admin/base/comm/person`
6. 完成了退出登录接口调用与客户端登录态清理
7. 完成了登录成功跳转、登录页回跳和路由白名单基础处理
8. 完成了应用启动后的用户信息自动补拉
9. 更新了工作台与顶部栏，展示当前登录用户和退出入口
10. 编写了 Step 4 开发记录文档

### 本步新增文档

- [08-登录认证开发记录.md](./08-登录认证开发记录.md)

### 真实联调结果

- `GET http://127.0.0.1:8001/admin/base/open/captcha`：返回 `code = 1000`，可正常拿到 `captchaId + base64 svg`
- `GET http://127.0.0.1:8001/admin/base/comm/person` 在无 token 下：返回 `HTTP 401`，响应体为 `{"code":1001,"message":"登录失效或无权限访问~"}`
- 后端本地初始化数据中确认存在 `admin` 用户，默认密码哈希对应 `123456`

### 验证结果

- `pnpm lint`：通过
- `pnpm build`：通过

### 过程说明

- `pnpm build` 在沙箱内依旧触发 `spawn EPERM`
- 之后在放开环境中完成了生产构建校验
- 本步没有提前接入菜单权限和动态路由

### 当前结论

- 前端已经具备最基础的登录认证闭环能力
- 当前认证链路为：验证码 -> 登录 -> 存 token -> 拉当前用户 -> 进入工作台
- 401 登录失效已能统一清理登录态并回到登录页
- refresh token 自动续期队列仍留在后续更完整的权限联调步骤处理

### 下一步建议

- Step 5 接入真实菜单权限、动态路由与后台壳子联调
